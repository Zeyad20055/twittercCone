// ============================================================
// CONSTANTS - ثوابت المشروع
// ============================================================

// BACKEND NOTE: Change BASE_URL to your backend URL before connecting
export const BASE_URL = 'http://localhost:3000/api';

// Token keys in localStorage
export const TOKEN_KEY = 'twitter_clone_token';
export const USER_KEY = 'twitter_clone_user';

// Roles
export const ROLES = {
  ADMIN: 'admin',
  USER: 'user',
};

// Pagination
export const DEFAULT_PAGE_SIZE = 10;

// Tweet character limit
export const TWEET_MAX_LENGTH = 280;
